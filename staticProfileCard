import 'package:flutter/material.dart';
void main() {
runApp(const MyApp());
}
class MyApp extends StatelessWidget {
const MyApp({super.key});
@override
Widget build(BuildContext context) {
  return MaterialApp(
    home: Scaffold(
      appBar: AppBar(title: const Text('Keatons Profile')),
      body: Center(
        child: Container(
       color: Colors.orange,
        padding: EdgeInsets.all(10),
        child: Column(
        mainAxisSize: MainAxisSize.min,
          children: [
          CircleAvatar(radius: 50, /* backgroundImage */),
          SizedBox(height: 12),
          Text('Keaton McCauley', style: TextStyle(fontSize: 22,           fontWeight: FontWeight.bold)),
          Text('Computer Science Major'),
          
          Row(
          mainAxisAlignment: MainAxisAlignment.spaceEvenly,
          children: [
          _statBox('359', 'Followers'),
          _statBox('8', 'Projects'),
          _statBox('1948', 'Likes'),
],
),


 
              ],
            ),
          ),
         ),
        )
      );
    }
  }
Widget _statBox(String number, String label) {
return Column(children: [Text(number), Text(label)]);
}
